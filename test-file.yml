name: Testing PR to main branch!
on:
  pull_request:
    branches:
      - pr-test
jobs:
  build:
     runs-on: ubuntu-latest
     env:
       token: ${{ secrets.PAT }}
    steps:
       - name: Testing
         run: echo 'This is a test branch!'
